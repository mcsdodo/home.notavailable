1. Use Dockerfile.Caddy to build caddy image in the testing
2. use provided .env file for cloudflare credentials
3. update docker compose to use this image and mount the .env file. Update compose to include 