global:
  scrape_interval: 30s
  evaluation_interval: 30s
  external_labels:
    cluster: 'home-docker'

# Enable remote write receiver for Alloy agents
# No scrape configs needed - all metrics come via remote_write from Alloy
scrape_configs: []
